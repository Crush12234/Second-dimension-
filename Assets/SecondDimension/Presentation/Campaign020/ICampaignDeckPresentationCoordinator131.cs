namespace SecondDimension.Presentation.Campaign020
{
    // Optional capability: existing Campaign020 implementations and callers stay compatible.
    public interface ICampaignDeckPresentationCoordinator131
    {
        M1CommandResult StartOrResumeCampaignDeck131(string chapterId);
    }
}
